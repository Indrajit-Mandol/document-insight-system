services:
  - type: web
    name: document-insight-system
    env: python
    pythonVersion: "3.13.4"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port 10000
